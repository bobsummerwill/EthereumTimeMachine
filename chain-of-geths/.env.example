# Example local environment overrides for the chain-of-geths helper scripts.
#
# Copy to: chain-of-geths/.env (this file is gitignored)
# and adjust values for your deployment.

# Default VM public IP for remote deploy/ops scripts.
VM_IP=44.199.189.113

# Default SSH user.
VM_USER=ubuntu

# Default SSH key path for the Ubuntu VM.
SSH_KEY_PATH=$HOME/Downloads/chain-of-geths-keys.pem
