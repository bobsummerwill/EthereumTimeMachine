# Vast Homestead extender configuration

# Used by generate-identity.sh to deterministically derive:
# - nodekey
# - miner private key (unless you replace generated-files/miner-private-key.hex)
IDENTITY_SEED=EthereumTimeMachine:homestead:cutoff-1919999:vast-1

# Password used to encrypt the keystore file and to unlock the miner account at runtime.
MINER_PASSWORD=dev

# Optional: change per-block timestamp delta (seconds)
FAKETIME_STEP_SECONDS=1200
